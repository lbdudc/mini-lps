###<@ return data.basicData.languages
    .filter(function(lang) { return lang !== "es" && lang !== "gl"; })
    .map(function(lang) {
      return {
        fileName: lang + '/components/chartViewer.json',
        context: lang
      };
    });
>###
###< if (feature.ChartViewer) { >###
{
  "chartViewer": {
    "title": "Chart Viewer",
    "chartCustom": "Custom Chart",
    "chartEntity": "Select an Entity",
    "axisX": "X Axis",
    "axisY": "Y Axis",
    "exportChart": "Export",
    "chartType": "Chart type",
    "chartTypeLine": "Line",
    "chartTypeBar": "Bar",
    "chartTypePie": "Pie",
    "tabExplorer": "Explorer",
    "tabSaved": "My charts"
  }
}
###< } >###
